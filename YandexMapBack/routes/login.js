'use strict';
var express = require('express');
var router = express.Router();
var db = require('./entities/db');

/* GET home page. */
router.post('/signin', function (req, res) {
    db.
});



module.exports = router;
